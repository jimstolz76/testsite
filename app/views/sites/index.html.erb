<h1>These are the sites, yo</h1>

<table>
  <tr>
    <th>Name&nbsp;</th>
    <th>Address&nbsp;</th>
    <th>Updated&nbsp;</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sites.each do |site| %>
  <tr>
    <td><%= site.name %>&nbsp;</td>
    <td><%= site.address %>&nbsp;</td>
    <td><%= site.updated %>&nbsp;</td>
    <td><%= link_to 'Show', site %></td>
    <td><%= link_to 'Edit', edit_site_path(site) %></td>
    <td><%= link_to 'Destroy', site, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Site', new_site_path %>
